import java.util.Scanner;

public class Main {
	
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		int x,y;
		x = scanner.nextInt();
		y = scanner.nextInt();
		int x1,y1,x2,y2;
		x1 = y1 = 10;
		x2 = 200;
		y2 = 300;
		if(x > x1 && x < x2) {
			if(y > y1 && y < y2)
				System.out.printf("(%d,%d) is in the rect\n",x,y);
			else if(y == y1 || y == y2)
				System.out.printf("(%d,%d) is in the rect !!! \n",x,y);
			else
				System.out.printf("(%d,%d) is not in the rect\n",x,y);
		}
		else if(x == x1 || x == x2) {
			if(y > y1 && y < y2)
				System.out.printf("(%d,%d) is in the rect !!!\n",x,y);
			else if(y == y1 || y == y2)
				System.out.printf("(%d,%d) is in the rect !!! \n",x,y);
			else
				System.out.printf("(%d,%d) is not in the rect\n",x,y);				
		}
		else {
			System.out.printf("(%d,%d) is not in the rect\n",x,y);
		}
	}

}
