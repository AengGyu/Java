<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -%kvp-%msg%n</pattern>
        </encoder>
    </appender>
    <root level="INFO"> <!-- 로그를 보고 싶으면 DEBUG로, 아니면 INFO -->
        <appender-ref ref="STDOUT" />
    </root>
</configuration>